function solve(string, startIndex, count) {
    let result;

    result = string.substring(startIndex, startIndex + count);

    console.log(result);
}

solve('ASentence', 1, 8);
solve('SkipWord', 4, 7);